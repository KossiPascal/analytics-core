import { Router } from 'express';
import { Middelware } from "../middleware/auth";

const SyncRouter = Router();

//dashboards
                                                                                                                          
export = SyncRouter;
