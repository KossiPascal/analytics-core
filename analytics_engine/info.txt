1️⃣ AGRÉGATIONS SQL (COMPLET – ENTERPRISE)

✅ WHAT THIS FINAL VERSION SUPPORTS
Query capabilities
SELECT / WHERE / GROUP BY / HAVING
Multi-table JOINs (no ambiguous columns, ever)
Automatic aliasing
CTEs (WITH queries)
Subqueries
Indicators (reusable, versioned)
PIVOT / UNPIVOT
Time-series (daily / weekly / monthly / epi-week)
Window functions
Views & Materialized Views
Incremental MV refresh support
Platform / enterprise features
Indicator registry
Role-based permissions
Column-level security
Query cost limiter (row scan guard)
Explain plan guard
Audit-friendly SQL
React-friendly JSON DSL
Safe against column collisions




🔢 Agrégations numériques
Fonction	Description
COUNT(*)	Nombre de lignes
COUNT(col)	Non-null
SUM(col)	Somme
AVG(col)	Moyenne
MIN(col)	Minimum
MAX(col)	Maximum
STDDEV(col)	Écart-type
STDDEV_POP(col)	Écart-type population
STDDEV_SAMP(col)	Écart-type échantillon
VARIANCE(col)	Variance
VAR_POP(col)	Variance population
VAR_SAMP(col)	Variance échantillon

🎯 Agrégations conditionnelles (PostgreSQL)
Fonction
COUNT(*) FILTER (WHERE condition)
SUM(col) FILTER (WHERE condition)
AVG(col) FILTER (WHERE condition)
BOOL_AND(condition)
BOOL_OR(condition)

📊 Agrégations statistiques
Fonction
PERCENTILE_CONT(0.5)
PERCENTILE_DISC(0.5)
MODE() WITHIN GROUP
CORR(x, y)
COVAR_POP(x, y)
COVAR_SAMP(x, y)
REGR_SLOPE(y, x)
REGR_INTERCEPT(y, x)

🧮 Agrégations DISTINCT
Fonction
COUNT(DISTINCT col)
SUM(DISTINCT col)
AVG(DISTINCT col)

🧵 Agrégations texte
Fonction
STRING_AGG(col, ',')
JSON_AGG(col)
JSONB_AGG(col)
ARRAY_AGG(col)

⏱ Agrégations temporelles
Fonction
MIN(date)
MAX(date)
AGE(date)
DATE_TRUNC('month', date)
EXTRACT(YEAR FROM date)

🪟 Window Aggregations (BI PRO)
Fonction
SUM(col) OVER (...)
AVG(col) OVER (...)
COUNT(*) OVER (...)
ROW_NUMBER()
RANK()
DENSE_RANK()
LAG(col)
LEAD(col)
FIRST_VALUE(col)
LAST_VALUE(col)
NTILE(n)

2️⃣ OPÉRATIONS / EXPRESSIONS (TRÈS COMPLET)
🔍 Opérateurs de comparaison
=   !=   <>   >   <   >=   <=

🧠 Opérateurs logiques
AND   OR   NOT

📦 Opérateurs de liste
Opération
IN (...)
NOT IN (...)
ANY (...)
ALL (...)

🔎 Opérateurs texte
Opération
LIKE
ILIKE
NOT LIKE
SIMILAR TO
~ (regex)
~* (regex insensitive)
POSITION()
SUBSTRING()
LENGTH()
TRIM()
LOWER()
UPPER()

🧮 Opérations mathématiques
+   -   *   /   %   ^

Fonction
ABS()
ROUND()
CEILING()
FLOOR()
POWER()
LOG()
EXP()
SQRT()

🕰️ Opérations temporelles
Fonction
NOW()
CURRENT_DATE
CURRENT_TIMESTAMP
DATE_TRUNC()
EXTRACT()
AGE()
INTERVAL
+ INTERVAL '1 month'

🔀 CASE / CONDITIONAL
CASE
  WHEN condition THEN value
  ELSE value
END

🧩 NULL handling
Fonction
IS NULL
IS NOT NULL
COALESCE(a, b)
NULLIF(a, b)

🔁 SET OPERATIONS
Opération
UNION
UNION ALL
INTERSECT
EXCEPT

🔗 JOIN OPERATIONS
Type
INNER JOIN
LEFT JOIN
RIGHT JOIN
FULL JOIN
CROSS JOIN

🔄 SUBQUERIES / CTE
Type
WITH cte AS (...)
Scalar subquery
Correlated subquery
EXISTS
NOT EXISTS

🔃 PIVOT / UNPIVOT (Postgres style)
COUNT(*) FILTER (WHERE sex = 'male')

UNNEST(ARRAY[col1, col2])

🌍 GEO (PostGIS – si activé)
Fonction
ST_Distance()
ST_Within()
ST_Contains()
ST_Intersects()

📦 JSON / JSONB
Fonction
->
->>
JSONB_EXTRACT_PATH()
JSONB_ARRAY_ELEMENTS()

3️⃣ COMMENT L’UTILISER DANS TON SQL BUILDER
Exemple catalogue backend
AGGREGATIONS = {
    "count": "COUNT",
    "sum": "SUM",
    "avg": "AVG",
    "median": "PERCENTILE_CONT",
    "stddev": "STDDEV",
    "string_agg": "STRING_AGG"
}

OPERATORS = {
    "eq": "=",
    "neq": "!=",
    "gt": ">",
    "lt": "<",
    "in": "IN",
    "like": "LIKE",
    "between": "BETWEEN"
}
