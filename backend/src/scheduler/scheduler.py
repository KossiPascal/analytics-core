# from apscheduler.schedulers.background import BackgroundScheduler
# from scheduler.jobs import refresh_forecast

# scheduler = BackgroundScheduler()
# scheduler.add_job(refresh_forecast, "interval", minutes=5)
# scheduler.start()
